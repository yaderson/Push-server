{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon -e '*.js' --ignore '*.json' index.js",
    "generate-vapid": "npx web-push generate-vapid-keys --json > vapid.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "urlsafe-base64": "^1.0.0",
    "web-push": "^3.4.4"
  }
}
